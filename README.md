# DOM Greeting & Color Box Project

This is a simple beginner-friendly project built using **HTML, CSS, and JavaScript** to demonstrate **DOM manipulation** and **event handling**.

---

## 📌 Project Features

- Displays a greeting message (`Hello`) by default
- Takes user input (name) and updates the greeting dynamically
- Four clickable boxes:
  - Red
  - Blue
  - Green
  - Yellow
- Clicking a box changes its background color using DOM
- Clean and responsive UI layout

---

## 🛠️ Technologies Used

- **HTML5** – Page structure  
- **CSS3** – Styling and layout  
- **JavaScript (DOM)** – Interactivity and dynamic behavior  

---

## 🎯 Concepts Covered

- DOM Selection:
  - `getElementById()`
  - `querySelectorAll()`
- Event Handling:
  - `addEventListener()`
- Dynamic Content Update:
  - `textContent`
  - `style.backgroundColor`
- Custom Attributes:
  - `data-color`

---

## 🚀 How It Works

1. User enters their name in the input field
2. Clicking **Greet** updates the heading to `Hello, [name]`
3. Clicking any color box fills it with the respective color
4. All actions are handled using **JavaScript DOM manipulation**

---

## 📂 Project Structure

